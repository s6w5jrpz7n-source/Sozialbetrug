{
  "name": "sozialbetrug",
  "version": "1.0.0",
  "private": true,
  "description": "Sozialbetrug: Arbeitslos zum Millionär",
  "scripts": {
    "build:www": "node tools/build-www.mjs",
    "sync": "npm run build:www && npx cap sync",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
